import * as React from 'react';

interface IProviderSliceProps {
}

const ProviderSlice: React.FunctionComponent<IProviderSliceProps> = (props) => {
  return (
      <div>
          
      </div>
  )
};

export default ProviderSlice;
